{
  "name": "server",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:migration:create": "wrangler d1 migrations create example_database",
    "db:migration:generate": "prisma migrate diff --from-empty --to-schema-datamodel ./prisma/schema.prisma --script"
  },
  "dependencies": {
    "@prisma/adapter-d1": "^6.13.0",
    "@prisma/extension-accelerate": "^2.0.2",
    "hono": "^4.9.0"
  },
  "devDependencies": {
    "prisma": "^6.13.0",
    "wrangler": "^4.4.0"
  }
}
